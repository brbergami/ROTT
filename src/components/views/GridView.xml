<?xml version="1.0" encoding="utf-8" ?>
<component name="GridView" extends="Group" initialFocus="posterGrid" >
    <interface>
        <field id="close" type="boolean" value="false" />
        <field id="imdbidSelected" type="string" />
    </interface>
    <children>
        <Label
            id="titleLabel"
            />
        <Label
            id="descriptionLabel"
            />
        <MiniKeyboard
            id="keyboard"
            />
        <!-- TBD: Upgrade this into a more customizable grid component in the future -->
        <PosterGrid
            id="posterGrid"
            />
        <Label
            id="gridInfoLabel"
            />
        <Timer
            id="keyboardTimer"
            />
    </children>
</component>
