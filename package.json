{
  "name": "brighterscript-template",
  "version": "1.0.0",
  "description": "A Roku application written in BrighterScript",
  "scripts": {
    "build": "bsc --createPackage=false",
    "watch": "npm run build -- --watch",
    "build-tests": "npx bsc",
    "package": "bsc --create-package"
  },
  "license": "MIT",
  "devDependencies": {
    "brighterscript": "^0.67.5",
    "rooibos-roku": "^5.12.0"
  }
}
